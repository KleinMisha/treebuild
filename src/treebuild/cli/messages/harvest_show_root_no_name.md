Flag `--show-root` selected, but no name for the root set. 
Rendering the tree as if `--show-root=False` 

To show the root name at the top of the rendered tree

```bash
treebuild harvest --root <name> --show-root
```

---- 

